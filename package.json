{
  "name": "repex-types",
  "version": "0.0.12",
  "description": "Interface for REPEX",
  "main": "dist/Index.js",
  "typings": "dist/Index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --write lib/**/*.{ts,tsx,json}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^1.10.2",
    "typescript": "^2.7.1"
  },
  "dependencies": {
    "io-ts": "^1.0.5",
    "ts-node": "^5.0.1"
  }
}
